version: '3.8'

services:
  app:
    image: messagesilo/app
    ports:
      - "3000:80"
    build:
      context: .
      dockerfile: src/MessageSilo.App/Dockerfile
    depends_on:
      - api
    networks:
      network:
        ipv4_address: 10.5.0.5

  api:
    image: messagesilo/api
    ports:
      - "5000:80"
    environment:
      Silo__PrimaryAddress: "10.5.0.3"
    build:
      context: .
      dockerfile: src/MessageSilo.API/Dockerfile
    depends_on:
      - silo
    restart: always
    networks:
      network:
        ipv4_address: 10.5.0.4

  silo:
    image: messagesilo/silo
    ports:
      - "11111:11111"
      - "30000:30000"
    environment:
      Silo__PrimaryAddress: "10.5.0.3"
      Silo__StateUnlockerKey: "test"
      Silo__DatabaseConnectionString: "mongodb://10.5.0.2:27017"
      Silo__DatabaseName: "messagesilo-local"
      AI__ApiKey: ""
    build:
      context: .
      dockerfile: src/MessageSilo.Silo/Dockerfile
    depends_on:
      - database
    networks:
      network:
        ipv4_address: 10.5.0.3

  database:
    image: mongo:6-jammy
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=asdqwe
    volumes: 
      - ~/mongo/data:/data/db
    networks:
      network:
        ipv4_address: 10.5.0.2

networks:
  network:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.0.0/16
          gateway: 10.5.0.1